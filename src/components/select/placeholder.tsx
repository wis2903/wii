import React from 'react';
import styles from './styles.module.scss';

const SelectPlaceholder = (): JSX.Element => {
    return (
        <div className={styles.placeholder} />
    );
};

export default SelectPlaceholder;